<Window x:Class="UITestKit.Views.MiddlewareView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:model="clr-namespace:UITestKit.Model"
        mc:Ignorable="d"
        Title="Middleware Log Viewer"
        Width="950" Height="600"
        Background="White"
        Foreground="Black"
        WindowStartupLocation="CenterScreen">

    <DockPanel Margin="10">
        <TextBlock Text="Middleware Logs"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding LoggedRequests}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F3F3F3"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#D0D0D0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="150"/>
                <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="120"/>
                <DataGridTextColumn Header="URL / Direction" Binding="{Binding Url}" Width="150"/>
                <DataGridTextColumn Header="DataType" Binding="{Binding DataType}" Width="100"/>
                <DataGridTextColumn Header="Request Body" Binding="{Binding RequestBody}" Width="250"/>
                <DataGridTextColumn Header="Response Body" Binding="{Binding ResponseBody}" Width="250"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusCode}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
